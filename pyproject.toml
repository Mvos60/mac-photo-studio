[project]
name = "mac-photo-studio"
version = "0.2.0"
description = "Verified Linux photography ingestion and provenance workflow for digiKam and darktable"
requires-python = ">=3.10"
dependencies = ["PyYAML>=6.0"]
